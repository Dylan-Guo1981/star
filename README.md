# star
一个可以直观查看天体运行的网站。

## 前端示例

`frontend/` 目录包含一个无需构建步骤的 React + Three.js 演示，用于展示太阳、行星及主要卫星的层级轨道。要运行演示：

```bash
cd frontend
python -m http.server 4173
```

然后在浏览器打开 [http://localhost:4173](http://localhost:4173) 即可体验可交互的轨道场景。
